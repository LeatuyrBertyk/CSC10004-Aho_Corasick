#ifndef BENCHMARK_H
#define BENCHMARK_H

#include <string>
#include <vector>
using namespace std;

void runBenchmarkFixK(const vector<string>& dict);
void runBenchmarkFixN(const vector<string>& dict);

#endif  // BENCHMARK_H